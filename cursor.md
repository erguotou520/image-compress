1. 

```txt
我现在想基于tauri react unocss antd 来设计一个图片压缩工具，请帮我完成这个庞大的工程。
第1步我们先设计页面组件，软件打开后页面中没有待压缩图片，此时提供一个空白组件，该组件提示用户直接拖动文件进来开始压缩
第2步 整个页面支持拖拽，当拖动文件后列出所有选择的图片文件和文件夹下的所有图片文件（递归查找）
第3步 开发MediaItem组件，该组件参考设计图实现，使用unocss设计样式，该组件包含一个压缩状态 文件名 文件完整路径（不显示） 文件后缀（不显示） 文件体积大小 以及压缩减少的体积大小。同时展开后可以设置压缩的一些配置，包括重新调整的宽高，可以通过锁按钮锁住宽高比，锁点击可切换状态，同时可以调整输出格式类型，默认为文本本身的输出格式，同时可添加其它输出格式（sharp提供哪些就可选哪些），还可以设置图片压缩比，从10%到100%，还可以配置是否覆盖原文件（默认覆盖），如果不覆盖，导出文件名则添加-compressed。整个组件第一行可以点击切换展开/收起状态
第4步 点击开始压缩按钮，将图片路径和压缩配置入队列，队列中一次最多处理cpu核心*2个任务，这些任务将发送到rust后端，由rust调用libvips来实现压缩，压缩的实现可以暂时先放，当压缩完成或失败时返回给前端，前端收到后将任务从队列中移除并继续后续的任务，直到全部完成
补充：压缩按钮旁边还有个下拉框按钮，点开是“选择统一输出到指定目录”的按钮，选择该按钮后将弹出系统保存目录选择框，后续的所有的压缩后文件都输出到该目录，原文件保持不动，即使勾选了覆盖原文件
第5步 系统设置页面，点击显示弹框，可以配置系统主题色，系统主题（light/dark/系统自动），这些配置都存储到localStorage并使用antd v5的主题系统实现，可以设置默认的压缩比以及未来更多的设置

请严格按照设计稿design.png来设计，使用unocss原子类来设计样式，使用antd v5来设计组件，在业务细节上也要考虑周全实现
```
